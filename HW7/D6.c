/*
 * D6.c
 * 
Строка наоборот
Дана строка заканчивающаяся символом точка '.' Напечатать строку наоборот. 
Реализуйте рекурсивную функцию, которая считывает и печатает строку наоборот.
void reverse_string()

Данные на входе: 	Строка из английских букв и знаков препинания. В конце строки символ точка. 
Данные на выходе: 	Исходная строка задом наперед. 
 
 */


#include <stdio.h>

void reverse_string()
{
    char c = getchar();  // Считываем символ
    
    if (c == '.')        // Базовый случай: если символ — точка, завершаем рекурсию
        return;
    
    reverse_string();    // Рекурсивный вызов (считываем следующий символ)
    putchar(c);          // Печатаем символ после возврата из рекурсии (в обратном порядке)
}

int main()
{
    reverse_string();    // Запускаем рекурсивный разворот
    return 0;
}


