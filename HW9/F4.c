/*
 * F4.c
 * 
 Цифры в строке
Написать функцию и программу, демонстрирующую работу данной функции.
Вывести в порядке возрастания цифры, входящие в строку. 
Цифра - количество. Функция должно строго соответствовать прототипу:

void print_digit(char s[])

Данные на входе: 	Строка из английских букв, пробелов, знаков препинания и цифр 
Данные на выходе: 	Функция должна принимать на вход строку и выводить на печать по формату: Цифра пробел количество. 

Пример
Данные на входе: 	Hello123 world77. 
Данные на выходе: 	1 1 2 1 3 1 7 2 

 */


#include <stdio.h>
#include <ctype.h>

void print_digit(char s[]) {
    int digit_counts[10] = {0}; // Массив для подсчета цифр (0-9)
    
    // Подсчет цифр в строке
    for (int i = 0; s[i] != '\0'; i++) {
        if (isdigit(s[i])) {
            int digit = s[i] - '0'; // Преобразуем символ в цифру
            digit_counts[digit]++;
        }
    }
    
    // Вывод результатов в порядке возрастания цифр
    for (int digit = 0; digit < 10; digit++) {
        if (digit_counts[digit] > 0) {
            printf("%d %d ", digit, digit_counts[digit]);
        }
    }
}

int main() {
    char input[1000];
    
    
    fgets(input, sizeof(input), stdin); // Чтение строки с консоли
    
    print_digit(input);
    
    return 0;
}
