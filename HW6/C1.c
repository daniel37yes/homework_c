/*
 *Модуль числа
Составить функцию, модуль числа и привести пример ее использования. 

 */
#include <stdio.h>

int abs(int num)
{
    //~ num =  abs (num);
    return abs (num);
}

int main()
{
    int num;
    scanf("%d",&num);
    printf("%d",abs(num));
    return 0;
}
