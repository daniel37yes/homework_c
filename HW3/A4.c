#include <stdio.h>

int main(int argc, char **argv)
{
int a, b, c, x;
	scanf("%d%d%d", &a, &b, &c);
	x=a+b+c;
	printf ("%d+%d+%d=%d \n", a, b, c, x );
	x=a*b*c;
	printf ("%d*%d*%d=%d \n", a, b, c, x );
return 0;
}

